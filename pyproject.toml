[tool.uv.workspace]
members = ["packages/core", "packages/cli"]

[tool.uv]
dev-dependencies = [
    "pytest>=8.0",
    "pytest-cov",
    "pytest-benchmark",
    "black",
    "isort",
    "flake8",
    "mypy",
]

