name: DB Build Tests - dev

on:
  push:
    branches: [dev]
    paths:
      - "packages/db_build/**"
      - ".github/workflows/db-build-tests-dev.yml"
  pull_request:
    branches: [dev]
    paths:
      - "packages/db-build/**"
      - ".github/workflows/db-build-tests-dev.yml"

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [macos-latest, ubuntu-latest, windows-latest]
        python-version: ["3.12", "3.13", "3.14"]

    defaults:
      run:
        working-directory: .

    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: uv sync --all-packages

      - name: Run tests
        working-directory: .
        run: uv run pytest ./packages/db_build/tests/ -v
